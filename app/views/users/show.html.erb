<% provide(:title, @user.name) %>
<div class="row">
  <div class="col-md-8">
      <p><%= gravatar_for @user %></p>
      <h1> Welcome, <%= @user.name %></h1>
      <h3>Email: <%= @user.email %></h3>
    <%= link_to "Update Profile", edit_user_path(current_user), class: 'btn btn-md btn-primary' ,style: 'color: ;'%>
    <%= link_to "My Orders", orders_path, class: 'btn btn-md btn-success' ,style: 'color: ;'%>
  </div>
</div>